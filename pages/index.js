import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'
import Header from './components/header/header'
import Post from './components/post/post'
import apiData from './api/posts.json'

function Home(props) { 
  const {value, navPosition} = props
  const renderPosts = () =>{
    return props.data.map(post=>{  
      const {title, content, img, id}  = post 
      return(<Post key={post.title} id={id} title={title} content={content} img={img} />)
    })
  }
  return (
    <div className={styles.container}>
      <Head>
        <title>TripJournal - Home</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header />
      <main>
        {
          renderPosts()
        }        
      </main>
    </div>
  )
}
export default Home
export async function getStaticProps() {  
  const data = apiData
  console.log(data)  
  return {
    props: data
  }
}
